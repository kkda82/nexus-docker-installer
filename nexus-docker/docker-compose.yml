services:
  nexus:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nexus-node
    environment:
      - TZ=${TZ}
    volumes:
      - ${HOME_DIR}/.nexus:/root/.nexus
    network_mode: "none"   # ✅ 추가
    tty: true
    stdin_open: true
